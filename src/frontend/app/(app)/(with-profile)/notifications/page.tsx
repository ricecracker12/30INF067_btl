import { NotificationList } from "@/features/notification/notification-list"

// Mọi thông báo của người đang đăng nhập (GĐ6 E3). Chỉ ráp (Đ-E13).
export default function NotificationsPage() {
  return <NotificationList />
}
