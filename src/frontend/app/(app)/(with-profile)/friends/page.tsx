import { FriendsScreen } from "@/features/friend/friends-screen"

// `/friends` (GĐ4 E3). Chỉ ráp (Đ-E13). Dưới `(with-profile)`: chưa có hồ sơ thì chưa có quan hệ nào để xem.
export default function FriendsPage() {
  return <FriendsScreen />
}
