# Biến cho stack ops (Grafana, C4). Trên VM: `cp .env.example .env` trong ~/app/ops/ rồi điền — Compose tự đọc .env
# cùng thư mục với file compose. KHÔNG đặt ở ~/app/deploy/ (đó là .env của staging). Giá trị thật KHÔNG commit.
#
# Sinh mật khẩu: openssl rand -hex 24
GRAFANA_ADMIN_PASSWORD=
